// import { selectWeatherForecastDomain } from '../selectors';

describe('selectWeatherForecastDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
